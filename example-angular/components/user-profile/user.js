'use strict';

angular.module('lightApp')
.controller('UserCtrl', ['$scope', '$routeParams', '$http', 'toaster', 'modelDataService', 'authService', function($scope, $routeParams, $http, toaster, modelDataService, authService) {
}]);
